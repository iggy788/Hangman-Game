var userChoice = '';
var wordList = ['foo', 'bar', 'baz'];

// choose a random word
// -- Math.floor(Math.random() * wordList.length); <-- a random number for
// array!

// display all the dashes for a given word
var chosenWord = wordList[0]; // <-- this should be random! See above :P
for (var i = 0; i < chosenWord.length; i++) {
    console.log(chosenWord);
}
// I have my dashed word already, take the user guess (EVENTS!) and
// any dashes with the correct letter
var dashedWord = "----";
userChoice = ? ? ?
    dashedWord[2] = userChoice;